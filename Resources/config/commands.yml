services:
  # Limpiar base de datos
  Maximosojo\ToolsBundle\Command\DataBaseClearCommand:
      tags:
            - { name: 'console.command', command: 'mtools:database:clear' }
  # Limpiar carpetas
  Maximosojo\ToolsBundle\Command\FolderClearCommand:
      tags:
            - { name: 'console.command', command: 'mtools:folder:clear' }